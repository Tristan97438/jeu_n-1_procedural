using UnityEngine;

public class score : MonoBehaviour
{
    [SerializeField] TMPro.TextMeshProUGUI compteur_txt;
    [SerializeField] GameObject player;
    [SerializeField] GameObject ennemi;

    //1) on augmente le score dès le lancemant
    //2) si un certain palier atteint le joueur a gagner
    //3) si le joueur touché par l'ennemi vant
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
